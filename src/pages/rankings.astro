---
import Layout from '../layouts/Layout.astro';

const RAWG_API_KEY = import.meta.env.RAWG_API_KEY;
let zeldaImage = '/images/games/trileros-preview.png'; // Imagen por defecto

if (RAWG_API_KEY) {
  try {
    // Buscamos juegos de Zelda, ordenados por mejor Metacritic y tomamos solo el primero
    const response = await fetch(`https://api.rawg.io/api/games?key=${RAWG_API_KEY}&search=The+Legend+of+Zelda&ordering=-metacritic&page_size=1`);
    const data = await response.json();
    if (data.results && data.results.length > 0 && data.results[0].background_image) {
      zeldaImage = data.results[0].background_image;
    }
  } catch (error) {
    console.error("Error al obtener la imagen de Zelda desde RAWG:", error);
  }
}

const rankings = [
  {
    title: 'Ranking de Juegos de Zelda',
    url: '/rankings/ZeldaParallax',
    image: zeldaImage,
    description: 'Un ranking de los mejores juegos de The Legend of Zelda ordenados por Metacritic.'
  },
    {
    title: 'Ranking de Juegos de Switch',
    url: '/rankings/switch2',
    image: '/images/games/trileros-preview.png', // Replace with a relevant image
    description: 'Un ranking de los mejores juegos de The Legend of Zelda ordenados por Metacritic.'
  },
  {
    title: 'Ranking de Donkey Kong',
    url: '/rankings/dk',
    image: '/images/games/trileros-preview.png',
    description: 'La historia de Donkey Kong clasificada por la crítica.'
  }
];
---

<Layout title="Rankings de Videojuegos">
  <main class="container mx-auto px-4 py-12">
    <div class="text-center mb-12">
      <h1 class="text-4xl md:text-5xl font-extrabold text-gray-900 dark:text-white mb-4">Rankings de Videojuegos</h1>
      <p class="text-lg text-gray-600 dark:text-gray-300">Explora nuestros rankings de videojuegos.</p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {rankings.map(ranking => (
        <a href={ranking.url} class="block bg-white dark:bg-gray-800 rounded-xl shadow-lg overflow-hidden transition-all duration-300 ease-in-out hover:shadow-2xl hover:-translate-y-2">
          <img src={ranking.image} class="w-full h-48 object-cover" alt={`Vista previa del ranking ${ranking.title}`} />
          <div class="p-6">
            <h5 class="text-xl font-bold text-gray-900 dark:text-white mb-2">{ranking.title}</h5>
            <p class="text-gray-700 dark:text-gray-400">{ranking.description}</p>
          </div>
        </a>
      ))}
    </div>
  </main>
</Layout>
